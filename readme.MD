# dRun-Q1 / SparePart Verify POC

Verify consistency between **Thai Customs Form (ใบขนฯ)**, **Invoice**, and **Bill of Lading (B/L)**.

## 🚀 Quick Start

```bash
git clone https://github.com/yourname/sparepart-verify-poc.git
cd sparepart-verify-poc

python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/macOS

pip install -r requirements.txt
streamlit run web_app.py